#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main() 
{
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    char ch; 
    char s[90];
    char sen[90];
    scanf("%c",&ch);
    scanf("%s", s);
    scanf("%s",&sen);
    printf("%c",ch);
    printf("%s",s);
    printf("%s",sen);
    return 0;
}
